

// 1) length - returns the number of elements in the array. It does not modify the original array.
// it includes space and special characters also

// const automation = "playwright"

// console.log(automation.length);

// 2) toUpperCase() - returns a new string with all characters converted to uppercase. It does not modify the original string.

// const automation = "playwright"

// console.log(automation.toUpperCase());

// 3) toLowerCase() - returns a new string with all characters converted to lowercase. It does not modify the original string.

// const automation = "PLAYWRIGHT"

// console.log(automation.toLowerCase());

// 4) includes() - checks if a string contains a specified substring. It returns a boolean value. It does not modify the original string.

// case sensitive

// const automation = "playwright"

// console.log(automation.includes("Wright")) // true

// 5) indexOf() - returns the index of the first occurrence of a specified substring in a string. It returns -1 if the substring is not found. It does not modify the original string.

// const automation = "playwright"

// console.log(automation.indexOf("P")) // 4


// 6) slice() - extracts a section of a string and returns it as a new string. It does not modify the original string.

// last index is not included

// const automation = "playwright"

// console.log(automation.slice(0, 4));


// 7) replace() - returns a new string with some or all matches of a pattern replaced by a replacement. It does not modify the original string.

// const str = "hello world"

// console.log(str.replace("hello", "Hi"));

// 8) replaceAll() - returns a new string with all matches of a pattern replaced by a replacement. It does not modify the original string.

// const str = "hello world hello"

// console.log(str.replaceAll("hel", "Hi"));

// 9) trim() - removes whitespace from both ends of a string. It does not modify the original string.

// const str = " hello "

// console.log(str.trim());

// console.log(str);

// 10) charAt() - returns the character at a specified index in a string. It does not modify the original string.

// const automation = "play wright"

// console.log(automation.charAt(4));


// 12 concat() - concatenates two or more strings and returns a new string. It does not modify the original strings.

// const str1 = "hello"
// const str2 = "world"

// console.log(str1 +" "+ str2);

// console.log(`${str1} ${str2}`);

// console.log("str1==>" + str1);


// 12) startwith() - checks if a string starts with a specified substring. It returns a boolean value. It does not modify the original string.

// const automation = "playwright"

// console.log(automation.startsWith('t'));

// // 13 endsWith() - checks if a string ends with a specified substring. It returns a boolean value. It does not modify the original string.

// const automation1 = "playwright"

// console.log(automation1.endsWith('p'));


// 14) split() - splits a string into an array of substrings based on a specified separator. It does not modify the original string.

const automation = "play wright"

console.log(automation.split("")) // [t, h, g, i, r, w, y, a, l, p]




